name: driver_extensions
description: A new Flutter project.

version: 1.0.0

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_driver:
    sdk: flutter

  colorize: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter